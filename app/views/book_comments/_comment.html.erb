<%= form_with model: [book, book_comment], url: book_book_comments_path(book.id), local: false do |f| %>
  <%= f.text_area :comment, placeholder: "コメントをここに", class: 'form-control', size: "20x5" %>
  <%= f.submit "送信" %>
<% end %>